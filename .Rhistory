knitr::opts_chunk$set(echo = TRUE)
source("sample_two_groups.R")
# Replace this comment with code required for Try It 3. (Remember that this text is a comment, so R ignores it; you can delete it if you want.) If you don't delete it, **start your code on a new line that doesn't start with #**
student_survey <- read.csv("student_survey.csv", stringsAsFactors = T)
s <- sample_two_groups(student_survey)
rt <- rowTable(s)
rt
phat <- replicate(1000, {
s <- sample_two_groups(student_survey)
rt <- rowTable(s)
rt[2, 2] - rt[2, 1]
})
hist(phat)
# Replace this comment with code required for Try It 3. (Remember that this text is a comment, so R ignores it; you can delete it if you want.) If you don't delete it, **start your code on a new line that doesn't start with #**
student_sample <- student_survey[sample(1:nrow(student_survey), size = 150, replace = F), ]
# Replace this comment with code required for Try It 3. (Remember that this text is a comment, so R ignores it; you can delete it if you want.) If you don't delete it, **start your code on a new line that doesn't start with #**
student_sample <- student_survey[sample(1:nrow(student_survey), size = 150, replace = T), ]
rowTable(student_sample)
.560.78
sum(phat >= .13) + sum(phat <= -.13)
phat <- replicate(1000, {
s <- sample_two_groups(student_survey)
rt <- rowTable(s)
rt[2, 2] - rt[2, 1]
})
summary(phat)
hist(phat)
phat <- replicate(1000, {
s <- sample_two_groups(student_survey)
rt <- rowTable(s)
rt[2, 2] - rt[2, 1]
})
hist(phat)
rt
rt[2, 2] - rt[1, 2]
phat <- replicate(1000, {
s <- sample_two_groups(student_survey)
rt <- rowTable(s)
rt[2, 2] - rt[1, 2]
})
hist(phat)
sum(phat >= .13) + sum(phat <= -.13)
# Replace this comment with code required for Try It 3. (Remember that this text is a comment, so R ignores it; you can delete it if you want.) If you don't delete it, **start your code on a new line that doesn't start with #**
student_sample <- student_survey[sample(1:nrow(student_survey), size = 150, replace = T), ]
obsDiff <- rt[2, 2] - rt[1, 2]
# Replace this comment with code required for Try It 3. (Remember that this text is a comment, so R ignores it; you can delete it if you want.) If you don't delete it, **start your code on a new line that doesn't start with #**
student_sample <- student_survey[sample(1:nrow(student_survey), size = 150, replace = T), ]
rt <- rowTable(student_sample)
obsDiff <- rt[2, 2] - rt[1, 2]
obsDiff
# Replace this comment with code required for Try It 9. (Remember that this text is a comment, so R ignores it; you can delete it if you want.) If you don't delete it, **start your code on a new line that doesn't start with #**
# HINT: you might want to use some code from lab 7 to help you with this.
phat <- replicate(1000, {
s <- sample_two_groups(student_survey)
rt <- rowTable(s)
rt[2, 2] - rt[1, 2]
})
sum(phat >= -obsDiff) + sum(phat <= obsDiff)
hist(phat)
chisq.test(student_survey$job, student_survey$ceoSenator)
chisq.test(student_survey$job, student_survey$ceoSenator, correct = F)
